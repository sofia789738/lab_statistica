#funzione lineare generica

def f (x) :
    m = 2
    q = 5
    return (m*x + q)

def f_parametrizzata  (x, m, q) :
    return (m*x + q)

def f_2 (x) :
    a = 1
    b = 2
    c = 4
    return (a*x**2+ b*x + c)

def f_parametrizzata_2 (x, a, b, c) :
    return (a*x**2+ b*x + c)
    